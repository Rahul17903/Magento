<?php

declare(strict_types=1);

/**
 * @var \Magento\Framework\Escaper $escaper
 * @var \Codilar\ProductEnquiry\Block\EnquiryCollection $block
 */

$enquiries = $block->getEnquiries();
$enquiryCount = $enquiries->getSize();
?>

<div class="product-enquiry-page">

    <div class="product-enquiry-header">

        <div class="product-enquiry-heading">
            <span class="product-enquiry-eyebrow">
                <?= $escaper->escapeHtml(__('Customer Requests')) ?>
            </span>

            <h1>
                <?= $escaper->escapeHtml(__('Product Enquiries')) ?>
            </h1>

            <p>
                <?= $escaper->escapeHtml(
                    __('View and manage all product enquiry requests submitted by customers.')
                ) ?>
            </p>
        </div>

        <div class="product-enquiry-summary">
            <span class="product-enquiry-summary-label">
                <?= $escaper->escapeHtml(__('Total Enquiries')) ?>
            </span>

            <strong>
                <?= $escaper->escapeHtml((string) $enquiryCount) ?>
            </strong>
        </div>

    </div>

    <?php if ($enquiryCount): ?>

        <div class="product-enquiry-card">

            <div class="product-enquiry-card-header">

                <div>
                    <h2>
                        <?= $escaper->escapeHtml(__('Enquiry Requests')) ?>
                    </h2>

                    <span>
                        <?= $escaper->escapeHtml(
                            __('Customer product enquiry submissions')
                        ) ?>
                    </span>
                </div>

                <span class="product-enquiry-count">
                    <?= $escaper->escapeHtml((string) $enquiryCount) ?>
                    <?= $escaper->escapeHtml(__(' Requests')) ?>
                </span>

            </div>

            <div class="product-enquiry-table-wrapper">

                <table class="product-enquiry-table">

                    <thead>
                    <tr>
                        <th><?= $escaper->escapeHtml(__('ID')) ?></th>
                        <th><?= $escaper->escapeHtml(__('Customer')) ?></th>
                        <th><?= $escaper->escapeHtml(__('Email')) ?></th>
                        <th><?= $escaper->escapeHtml(__('Address')) ?></th>
                        <th><?= $escaper->escapeHtml(__('SKU')) ?></th>
                        <th><?= $escaper->escapeHtml(__('Qty')) ?></th>
                        <th><?= $escaper->escapeHtml(__('Submitted')) ?></th>
                    </tr>
                    </thead>

                    <tbody>

                    <?php foreach ($enquiries as $enquiry): ?>

                        <tr>

                            <td>
                                <span class="product-enquiry-id">
                                    #<?= $escaper->escapeHtml(
                                        (string) $enquiry->getId()
                                    ) ?>
                                </span>
                            </td>

                            <td>
                                <div class="product-enquiry-customer">
                                    <span class="product-enquiry-avatar">
                                        <?= $escaper->escapeHtml(
                                            strtoupper(
                                                substr(
                                                    (string) $enquiry->getName(),
                                                    0,
                                                    1
                                                )
                                            )
                                        ) ?>
                                    </span>

                                    <span class="product-enquiry-name">
                                        <?= $escaper->escapeHtml(
                                            $enquiry->getName()
                                        ) ?>
                                    </span>
                                </div>
                            </td>

                            <td>
                                <a
                                    class="product-enquiry-email"
                                    href="mailto:<?= $escaper->escapeHtmlAttr(
                                        $enquiry->getEmail()
                                    ) ?>"
                                >
                                    <?= $escaper->escapeHtml(
                                        $enquiry->getEmail()
                                    ) ?>
                                </a>
                            </td>

                            <td>
                                <span class="product-enquiry-address">
                                    <?= $escaper->escapeHtml(
                                        $enquiry->getAddress()
                                    ) ?>
                                </span>
                            </td>

                            <td>
                                <span class="product-enquiry-sku">
                                    <?= $escaper->escapeHtml(
                                        $enquiry->getSku()
                                    ) ?>
                                </span>
                            </td>

                            <td>
                                <span class="product-enquiry-qty">
                                    <?= $escaper->escapeHtml(
                                        (string) $enquiry->getQty()
                                    ) ?>
                                </span>
                            </td>

                            <td>
                                <span class="product-enquiry-date">
                                    <?= $escaper->escapeHtml(
                                        $enquiry->getCreatedAt()
                                    ) ?>
                                </span>
                            </td>

                        </tr>

                    <?php endforeach; ?>

                    </tbody>

                </table>

            </div>

        </div>

    <?php else: ?>

        <div class="product-enquiry-empty">

            <div class="product-enquiry-empty-icon">
                <span>?</span>
            </div>

            <h2>
                <?= $escaper->escapeHtml(__('No Enquiries Yet')) ?>
            </h2>

            <p>
                <?= $escaper->escapeHtml(
                    __('There are currently no product enquiry requests.')
                ) ?>
            </p>

        </div>

    <?php endif; ?>

</div>
